var db = require('../db/db');

module.exports = {
	insert:function(app){
//		app.get('/userHistory',function(request,response){
//			var userid = request.query.id;
//			var history = request.query.history;
//			var date = request.query.date;
//			db.createHistory(request.query,function(data){
//				response.send(data);
//			})
//		})
	}
}
